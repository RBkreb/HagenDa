# 主目标：对局系统
- 据点(HQ)与驻地(Garrison)
- 标记系统
- 对局输赢分数
- 小队系统
- 重新部署系统
## 基本构成
- 一场对局由敌对两方构成
- 每方最多含有6个小队，每个小队最多5人。总计30 vs 30
## 驻地(Garrison)
每一方拥有一个驻地GR，作为各自的安全区，无法被敌方占领，可以直接重新部署于GR内的部署点。进入敌对GR后10s会强制击杀，不可救援，所以可以短暂进入敌对GR。开局时将所有单位部署至各自GR
## 据点(HQ)
- 地图中可被反复争夺的点位，根据点位范围内双方人数计算每秒争夺值，未被争夺时保持中立
- 据点争夺值上限+-60，根据点内双方差值计算。例如红方5人，蓝方3人，设红方为负值，当前争夺值+10，争夺值变化为-2/s,经过35s，会到达-60下限，红方完全取得此据点
- 完全取得据点时，取得方可获得此据点的重新部署点位，并获得点位保护：发生争夺值变化时，仍可使用此据点的部署点位，直到点位争夺值发生符号变化。也就是争夺值为0时，重置为中立据点。
## 标记系统
- 相机射线不被遮挡的穿过敌方实体(烟雾透明度低于40可以判定为无遮挡)，点击Q给予敌方实体10s的标记效果。
- 标记效果：显示在小地图与大地图的红点中(UI系统未来加入，占位符)，被标记实体头部最高点向上0.1m显示圆形亮红点，红点不可穿过遮挡
- 未来会加入更智能的AI，预留被标记的tag
- 50m内小队成员互相可见头部最高点向上0.1m的绿色亮点，此两点可以穿过遮挡
- 50m内无遮挡的友方(非同小队)，可见头部最高点向上0.1m的蓝色亮点，不可穿过遮挡
## 对局输赢分散
- 开局两方为0分，先到达1000分的胜利，结束对局(测试阶段使用100分即可)
- 击杀敌人获得1分，完全争夺据点获得10分，拥有点位保护的据点每10s给予友方3分
## 重新部署系统
- 死亡10s后可重新部署
- 可以选择在GR部署点，已争夺HQ部署点,小队成员身边部署
- 一个HQ拥有多个部署点，选择HQ部署时自动部署到距离威胁最小的部署点。HQ自身包含"引力"(仅作逻辑计算，不作物理计算)，HQ 50m内的敌方实体含有斥力，选取与力方向最近的部署点
- 选择仍存活的小队成员，部署时选取小队成员2m内无遮挡位置部署

## 测试地图
100*200的长方形场景，四周使用刚体墙壁围起来，长边的两端有双方的GR，长方体中间含有2个HQ。
每边2个小队，每个小队2人，把player作为其中一人，AI填充剩余。AI死亡且没被救援则重新部署，随机选择GR或HQ。