# 主目标：完成通用枪械模板
# 枪械组成要素
- 射速
- 射击方式
- 子弹容量
- 子弹伤害
- 子弹飞行速度
- 换弹速度
- 后座力
- 子弹散布
- 从奔跑到开枪射击中的冷却
- 从非瞄准状态到完全瞄准状态间隔
## 射速
子弹射击间隔，默认600次/分钟
## 射击方式
- 全自动：按住开火键保持射击
- 半自动：按开火键只能射出1发子弹，必须松开并再次点击开火才能射击下一发
- 连射：按开火键能以射速射出3发子弹，必须松开并再次点击开火才能射击下一轮
- 栓动：按开火键只能进行一次射击，进行自动拉栓后可以进行下一次射击
- 每个枪可以拥有多种射击模式，默认全自动+连射+半自动，默认使用V键切换射击模式
### **为了确保性能，每个真人/AI在复活后获得1个子弹对象池(上限60个对象)，子弹命中任何物体后停止计算并禁用伤害，在对象池中持续批处理使失效子弹重新可用化，防止高频gc导致性能下降**
### 半自动射击与连射的射速不能高于枪械射速，防止高频点击开火键超过枪械射速
## 子弹容量
- 进行换弹后可以装填子弹
- 备弹：一个经典设计是30/150，30发已装填子弹，可以开火射击，150发备弹，进行换弹时从备弹中取出并装填，当备弹较少时，弹匣无法装满，无备弹则禁用换弹
## 子弹伤害
```
damage=max(base*(1-distance*decay)*part,min_base)
且 base>min_base
```
子弹命中敌方时造成的伤害，预留一个倍率变量(part)用于未来击中不同部位造成不同伤害的设计。
命中判定：采用射线法，前一tick与此tick子弹位置的连线所穿过的所有物体进行排序，对前一tick位置最近的物体作用命中判定。
每颗子弹记录它被射击时的位置，在发生命中敌方后计算距离distance，根据距离和衰减系数decay来计算伤害，且不小于最小值min_base。
默认base 30,min 15,decay 1/120,part 1
## 子弹飞行速度
子弹在射出后沿着射出方向持续运动，并持续受到减速度(默认-300m/s^2),初始速度(默认800m/s)
## 换弹速度
当弹匣内还有子弹时，快速换弹(默认2s)，当弹匣内子弹为0时，慢速换弹(默认2.5s)。
**需要预留打断机制，当角色进行切换武器，或者被击杀时，打断换弹，暂停计时，在完成会打断换弹的动作且武器正在被角色使用时，继续计时完成换弹。例如换弹时被击杀，被换弹的武器保持暂停计时，直到角色被救援**
## 子弹散布
- 以屏幕中心为轴，在范围内随机选择一个角度射出。
- 持续射击时逐渐扩大范围。
- 设定一个系数持续缩小范围
## 从非瞄准状态到完全瞄准状态间隔：瞄准时间
代表从非瞄准状态的大范围子弹散布缩小至完全瞄准状态，高精度子弹散布所需的时间，也代表枪械瞄准中心移动至屏幕中心所需时间，从瞄准->非瞄准则逐渐增大，枪械回到非瞄准状态
# 散布系统详解
每一次射击的散布都受多个值影响
- 当前状态最小散布，非瞄准状态(默认3°)->完全瞄准(默认0.1°)，中间线性减小，代表当前最小散布的下限
- 当前最大散布，非瞄准状态(默认10°)->完全瞄准(默认2°)，线性增大，代表散布上限
- 散布回复系数，默认7°/s，持续减小当前最小散布，直至到达当前状态最小散布
- 当前最小散布，每一次射击都增大固定值(默认0.5°)，直到到达当前最大散布
- 散布回复系数，射速和单次射击增大的值必须要确定，先确定射速，子弹散布在满速射击时必须持续增大，不要让散布回复系数过大导致子弹散布维持在低值
## 后座力
分为2种后座力，需要同时作用
### 屏幕后座
相机视角根随每次射击线性移动，有移动距离和移动角度。默认移动0.5°，角度90°，也就是竖直向上移动相机角度。
后座力回复系数，默认7°/s，计算方式可以类同散布回复系数
### 视觉后座
此后座不影响相机角度，让枪械模型进行轻微震动，让相机视角轻微震动
## 从奔跑到开枪射击中的冷却
默认(0.1s),冲刺时不能直接射击,角色按住开火键时间多于此值时才能开火，按住开火键后退出冲刺模式，只能低速移动。
当角色处于开火或瞄准状态时，无法进入冲刺状态