# 角色动画
- 混合树资源Assets/Scripts/Network/Animation/NetworkSoldierLayers.controller
- Fatui士兵预制体 Assets/Model/Fatui/Fatui with Collider.prefab
- M4枪模预制体 Assets/Model/Guns/M4_8.prefab
## 角色结构
角色主胶囊体，原先network中的胶囊体去除render显示
(角色模型，枪械模型基准(枪械模型))
## 角色模型结构
- L0 LocomotionFull   无 mask,八向混合树  
- L1 LocomotionLower  mask=下半身(腿+髋) 八向混合树
- L2 UpperActions     mask=上半身，禁止八向移动动画剪辑
- L3 Death            无 mask,死亡无出边,复活 Rebind。预期使用ragdoll，占位符
- Rig 
```
脚部贴地IK
手臂Two Bone IK
手HandGripConstraint(armIK=false)->约束于枪模
枪基准 Aim Constraint 源头头部骨骼子对象AimSource + 当前相机射线50m目标点
枪基准未初始化，建议放置右前胸，或摆好姿势后交由用户设置
```
## 姿态
- 上半身跟随胶囊体相机水平旋转
- 站:下半身T pose +  脚 IK 贴地 
- 蹲:multiPostionConstriant绑定角色骨骼根下降与上半身旋转 + 脚 IK 贴地 + 混合树继续
- 趴:剪辑冻结 + 膝/肘 地面IK;移动=身体极缓蹭行;multiPostionConstriant绑定角色骨骼根下降与上半身俯仰。此时上半身不能旋转，整个身体都是平趴状态，所以相机旋转时让整个身体跟着转
- 死亡
### 趴姿实现要点(实测)
- **旋转绕腹部**:bodyPivotHeight=0 → 自动取 DEF-spine 骨盆高度。绕脚根/膝盖会把头扫一大圈。
- 趴姿胶囊沿本地 +Z 放长并沿 Z 平移罩住全身(proneCapsuleLength=1.85, proneCapsuleForward=-0.02)。
  只用以实体原点为中心的 1.8m 立姿胶囊覆盖不到躯干 → 身体中段可穿过障碍物。
- 踝离地高度趴姿远小于站立:footHeight 0.21(立) → footHeightProne 0.03(趴),按 proneCur 插值。
  趴姿若沿用 0.21 会把脚抬起,髋-踝由 0.886 缩到 0.618 → 两骨 IK 屈膝把膝顶到髋上方。
- 脚部地面射线必须**排除自身碰撞体**(运动胶囊/命中箱/枪);否则先打到自己胶囊,目标被顶到
  胶囊表面 → 趴姿(胶囊沿 Z 放长)脚悬空、膝抬高。
## 静止脚位(静态基准)
静止时脚位 = 腿根 + **模型旋转 × T pose 下“腿根->踝”偏移**(footRestOffsetL/R,开机时量一次)。
- 该偏移随身体姿态旋转:站立=脚下、左右分开;趴下=身后、仍左右分开 → 站/蹲/趴同一套公式。
- **不可**用“当前动画踝下方”当脚位:那样 IK 输出会反馈回下一帧输入,导致自锁 ——
  蹲→趴时脚锁在屈膝帧(hipToAnkle 停在 0.52),趴→蹲时脚留在趴位(呈跪姿)。静态基准无此反馈。
- 转身滞后:静止转向时脚位绕实体竖轴按 lag = footLagYaw - dampedYaw **反向公转**
  (等价旧“冻结世界位置+按滞后角公转”,但不再需要冻结状态)。
## 肘/膝 IK pole(hint)专项
TwoBoneIK 的 hint 决定肘/膝弯曲平面,缺失或退化时关节会异常翻转。
- 预制体 `Hint` 是模型根的**直接子物体**(动画骨架之外),内含 4 个**静态**基准点:
  RtKnee/LtKnee/RtElbow/LtElbow。不随动画变形,只随整体 yaw/趴姿俯仰转动。
- hint 直接复用这 4 个点:位置恒可预测。
  **反例(已废弃)**:早期由驱动每帧按关节位置+方向重算 hint,会在部分姿势把 hint
  推到角色身后 → 肘绕错误平面弯曲(实测)。
- 每次 Bake 幂等:清理早期版本的 ElbowHint_R/L 空物体;ArmIK 的 hint 指向 Hint/*Elbow。
## 动作
- 跳: 轻微下蹲后回到混合树
- 滑铲:蹲姿，保持混合树冻结帧，脚IK，滑铲结束出边
- 滞空:停在滞空后0.1s的帧，让跳的下蹲播完伸直
## 脚部贴地IK专项
八向移动的动画剪辑中可以找到基础IK,Right Foot Q,Right Foot T,另一组是Left。在动画剪辑的脚落地帧，高度轴最低，对落地的脚启用地面IK,抬脚后禁用。IK使用animation rigging，或者射线检测，射线检测在脚骨上方，向下射0.1m，检查地面并启动IK。
站立/蹲/趴IK,三维固定；滑铲IK,仅y轴固定，让脚贴地滑行
### 蹲姿(含蹲行)必须满权重——实测
- 移动上限 footIKCapMoving=0.15(软钉,保留剪辑抬脚自由度),但**蹲姿不能沿用**:
  蹲下时模型根整体下沉 crouchOffset.y=-0.5,而剪辑仍是站立高度的走/跑,踝骨被一起带沉。
  0.15 权重只剩 15% 修正量 → 蹲行时脚穿地 **0.44m**(实测 ankleY≈-0.23,地面 y=0)。
- 故单列 footIKCapCrouch=1(蹲姿满权重)。
- 实测(四组合各 ~500 帧):蹲+走 由 [-0.230,-0.008] → [0.210,0.210] 穿地 0;
  站+走 保持 [0.195,0.417] 不变(站立本就不穿地,保留抬脚自由度)。

### IK 权重 = 上限 × 触地程度(落地开/抬脚关)
门控值 plant(相对最低踝高的自适应基线)此前**只被记录、未参与权重** ——
移动中是恒定软钉。现改为 `ik.weight = cap * plant`:
触地相 ≈ cap、摆动相 → 0(纯剪辑)。静止/蹲/滑铲 forcePlant=1 → 权重恒 = cap(持续贴地)。

### 蹲走必须程序化解算(约束会把脚钉死)——实测
**症状**:蹲姿移动时脚完全钉在地上,步幅几乎为 0。
**根因**:`TwoBoneIKConstraint` 的 IK 目标 XZ 取自 `tip.position`,而 tip 就是**约束
上一帧自己的输出**。权重=1 时"目标 XZ = 上一帧落点"构成**不动点** —— 剪辑把脚送到
哪都没用。实测蹲走:步幅 0.767→**0.031m**、大腿摆动 54°→**6°**、小腿 75°→**5°**。
(站姿靠 0.15 权重侥幸保留步幅 —— 85% 仍由剪辑驱动。这也解释了"站姿是间歇性开启 IK")
**做法**(`useCrouchWalkLegs`):蹲走时把脚约束权重按 `crouchLegProcCur` 压到 ~0,
改由 `ApplyCrouchWalkLegs()` 在 **LateUpdate** 程序化两骨 IK:
- 权重归零后读到的是**干净的剪辑姿态** → 目标 XZ 直接取剪辑踝位 XZ
  → 步幅与大小腿前后摆动**完全保留**(实测步幅 0.767、大腿 124°、小腿 89°)。
- 目标 Y = 地面 + 踝高 + **抬脚量**;抬脚量 = 剪辑踝相对本周期最低点的抬升
  → 触地相抬脚量≈0(脚钉在地面)、摆动相>0(脚离地),触地/抬脚天然分离。
  由此**不需要**额外加腿部旋转量:大小腿摆动完全来自剪辑。
- `crouchLiftMax` **必须 ≥ 蹲降量(0.5m)**,否则会切掉步态周期上半段(腿型被压扁)。
  实测 0.22 太小 → 0.7。
- 膝 pole 用**身体前向的水平分量**(膝必须朝前弯):固定且不随脚位变化。
  **不要**用剪辑膝/静态 Hint 点 —— 蹲姿下可能与"胯->踝"轴近共线,pole 退化会让膝
  瞬间翻面(实测小腿摆幅 234°,物理不可能)。
- 实测(各 ~570 帧,含膝翻转检测):蹲+走 ankClr=[0.210,0.449] 无穿地、kneeFlips=0;
  站+走/蹲+停/站+停 亦全部 kneeFlips=0。
## 转身专项
转身目的是让角色转向更自然，基本逻辑为保持脚部IK对地面锁定，让脚钉在地面，在特定条件下将IK定位点跟随旋转从而整体转身
- 当位于地面时，下半身启用混合树+脚IK。上半身立即跟随水平转向。下半身滞后，站/蹲静止状态上半身开始转向后1s，开始追赶转向。移动状态持续保持转向追赶。
- 转向追赶使用IK 基准点跟随旋转。
- 上半身脊柱反扭上限 60°，超出立即开始追赶
- 当位于空中时，上下半身保持立即同步水平旋转
- 趴下状态特殊处理
# 相机
不再使用绑定于网络角色胶囊体的相机，将相机放置于模型骨骼子对象 headcollider(一个球体)表面，注意是表面不是球心。让相机跟随球体移动。此处需要让网络代码同时兼容。注意：相机不仅要跟随坐标移动，还要时刻保持与球体表面法线平行，模拟在头部的眼睛
### 相机实测要点
- **瞄准近裁剪面**:ADS 时把 `playerCamera.nearClipPlane` 收到极小值(0.01)，否则枪身/瞄具被
  近裁剪面切掉 → 看见枪内部。按本地瞄准意图(clientAim)平滑过渡，走客户端本地、不经服务端回环。
- **俯视脱离 headcollider**:俯角超过 `downLookDetachStart`(55°)后，相机沿角色**水平正面**
  线性外推，到最大俯角时外推 `downLookDetachMax`。否则俯视会看到自己身体内部的头/躯干网格。
  - **量必须够大**:角色前向跨度约 0.44m，而相机在球面上只到约 0.12m →
    外推量需 >0.32 才能真正脱出身体轮廓。默认 0.50（实测 70° 起 exit body，
    80° 到 camZ=0.53 > bodyMaxZ=0.30）。取 0.35 时相机虽离球面(0.11→0.27)但仍
    在身体包围盒内，视觉上"看着没动"。
  - 俯仰约定是**正=低头**（见 Look 段 Tooltip）→ `downDeg = +renderPitch`。
## 枪械专项
- 腰射:枪基准 Aim Constraint 源头头部骨骼子对象AimSource + 当前相机射线50m目标点,禁用冲刺基准Aim Constriant SprintAim
- 瞄准:根据枪械开镜时间将枪械的RearAim和SightAim约束至相机射线，要求可调整瞳距，禁用Aim Constriant
- 冲刺:枪自身Aim Constriant 源 人物子对象SprintAim,左右摆枪(局部X)，禁用枪基准Aim Constraint
- 开火:枪模根据后座力数值发生震动，bolt根据射速 局部z 0.156前向界限，0.185后向界限，来回移动，最终停在前向界限。可以用动画剪辑。由于枪械基准存在，所以枪不会因为后座力而影响其他动作
- 换弹占位符
### 腰射/冲刺实测要点(重要)
- **腰射↔冲刺的朝向必须沿旋转路径混合（Slerp），不能交给约束的多源加权**。
  约束是把各源的 `(源位置−自身位置)` 向量**加权相加**后只做一次 LookRotation。
  腰射(相机前视)与冲刺(指向 SprintAim)的枪管方向实测相差 **123°**，两个大角度向量
  相加时方向在权重中段急剧扫过 → 实测单帧转 82°、枪位跳 0.50m（可见瞬移）。
  改为：`rot = Slerp(hipRot, sprintRot, sprintBlend)`，沿最短旋转路径过渡，与夹角无关。
  实测单帧降至 **0.032m / 6.1°**。
- **约束仍然在用**（不该停用）：把混合好的朝向**编码成单个源(driverAimTarget)的位置**，
  约束据此解出朝向并负责 SceneUp 滚转 —— 脚本只控制混合路径，不接管旋转。
  （曾试过让约束挂两个加权源，就是上面那条的翻车原因；也曾试过脚本完全接管旋转，
  则约束的 SceneUp 滚转会丢失。）
- **摆枪**（`sprintSwayLateral` 平移 + `sprintSwayAmplitude` 滚转，共用同一相位）：
  - **平移**：AimConstraint 瞄的是源的**位置**，所以只有平移 SprintAim 才会改变约束输出。
  - **滚转**：`sprintSwayAmplitude`（枪顶左右倾）**不能**靠转 SprintAim 实现 —— 约束的
    `worldUpType=SceneUp` 把滚转**钉在世界朝上**，改源朝向对枪零影响（实测 AMPL=20 时
    枪转动 0.000°，是死代码）。改为直接写在枪的局部旋转上、绕**枪管轴**施加。
- **腰射 = 枪托(Stock)在右肩 GunCylinder 柱面上滑动**，与相机在 headcollider 球面滑动同式:
  `stockTarget = 柱心 + 视线方向 × 半径`（**含俯仰** → 枪的俯仰角 = 相机俯仰角）。
  - 不能做水平投影（会把枪俯仰锁死在水平面）；不能用“眼空间固定偏移”（下俯会衍生
    指向身后的分量，枪托捅进后背）。柱面滑动几何有界。
  - 半径取 GunCylinder 的 Collider 尺寸（重调后会变，按实测读取）。
- **基准位置必须解析求解，不能用误差反馈**：约束在本帧稍后才改写基准旋转，反馈读到的
  `stock.pos` 属于上一帧旋转 → 大角度切换时先算错、下帧弹回（实测单帧跳变 0.49m）。
  改为：`basis.pos = 目标 − R·(Stock 基准局部偏移)`，R = 同一份 BlendedAimRotation；
  因方向依赖基准位置，迭代 3 次取不动点。
- **冲刺位置** = 腰射柱面点 + 三个偏移（都按 sprintBlend 淡入）。
  冲刺段全部用**去俯仰基**（见下条），故：横向 = `eye.right`、高度 = **世界 up**、
  后撤 = **水平前向**。
  | 字段 | 轴 | 作用 | 默认 |
  |---|---|---|---|
  | `sprintLateralOffset` | eye.right | 横向（负=向左，修“枪整体偏右”） | −0.07 |
  | `sprintHeightOffset` | 世界 up | **高度**（正=抬高，不随俯仰倾斜） | 0 |
  | `sprintDropback` | −水平前向 | 后撤 | 0.06 |
  偏移加在**枪托目标**上，不能加在“腰射基准位”上（后者是相对量，会重复计入当前状态）。
- **冲刺锁俯仰 + 去俯仰基**(实测,用户报告两点):见下节「冲刺俯仰/水平跟随专项」。
# 冲刺俯仰/水平跟随专项(实测)
1. **枪不再跟随相机俯仰**:`PitchLockedAim(dir, sprintBlend)` 把绕柱面滑动方向与冲刺瞄准
   方向的俯仰分量按 sprintBlend 淡出 → 枪管保持水平。实测相机俯仰 ±25° 时枪 pitch 恒
   **0.0°**;腰射则完全跟随(gun pitch == cam pitch,偏差 0.0°)。
2. **"水平跟随不同步"的真因是枪、不是上半身**。静止姿态(不播走路)下定速扫掠测量:
   胸 yaw 偏差 **0.0°**(上半身其实即时跟随)、枪 yaw 偏差 **±3.3°**。
   (走路时胸的 ±9.5° 是**动画自身摆动**,静止时即消失,不是跟随问题。)
   根因:冲刺偏移用了 `eye.forward / eye.up` —— 相机俯仰会改变枪托沿枪管轴/竖直方向的
   位置,而 `BlendedAimRotation` 里 `(SprintAim − 枪位)` 的方向随之摆动 → 枪的**水平**朝向
   被俯仰带偏(与相机俯仰同相)。改用**去俯仰基**(横向 `eye.right` / 高度世界 up /
   后撤水平前向)后,枪 yaw 偏差降到 **±0.65°**(即设计上的摆枪幅度)。
   → 结论:**上半身本来就是即时跟随**,漂移来自枪;修复后两者都即时跟随相机。
- 与腰射的区别:腰射**要**全俯仰跟随(`stockTarget = 柱心 + 视线方向 × 半径` 含俯仰),
  冲刺**不要**。两者按 sprintBlend 连续过渡,不会突变。
# 网络
所有动作由服务端权威状态触发。
# 调参工具(HagenDa/SoldierAnim/Apply Rig Tuning)
把推荐值写进预制体(Fatui rig 驱动 + NetworkPlayer/AIEntity 的控制器)。
**只填未手调的字段** —— 手调过的值不会被覆盖:
- 判定写不写:先看本工具**上次写过什么**(记录在 EditorPrefs,按预制体 GUID+类型+字段名索引)。
  当前值 == 上次写入值 → 仍归工具管辖 → 可写入新推荐值(改了脚本常量后重跑即传播)。
  当前值 != 上次写入值 → 有人手调过 → **跳过并报告**。
- 无记录(首次运行)→ 退化为"与脚本字段默认值比对":等于默认=没调过=可写;否则跳过。
- 需要无条件重置时用 `Apply Rig Tuning (Force Overwrite)`。
所以:手调后**不要**再跑普通版菜单(它会跳过你的值,不会覆盖);确实想重置才用 Force。
# 重要提醒
- 动画剪辑资源只使用8向混合树奔跑，其他资源不使用
- 当前natlan和Fatui均有各自的一条分支实现，这两条实现均为残缺型。
- 从0开始实现，不要混合两条分支的实现，这两条分支未经代码审查和测试
